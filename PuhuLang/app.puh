void main(){
	for(int i = 0; i < 10; i = i + 1){
		double start = clock();
		int x = fibb(32);
		double end = clock();
		double elapsed = end - start;
		printlnDouble((double)x);
		printlnDouble(elapsed);
	}
}

int fibb(int n){
	if(n > 1)
		return fibb(n - 1) + fibb(n - 2);
	else
		return 1;
}