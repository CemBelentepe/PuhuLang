void main(){
	double startTime = clock();
	double x = fact(30.0);
	double elapsed = clock() - startTime;
	println(x);
	print("Elapsed time: ");
	println(elapsed);
}

double fact(double n){
	if(n > 1.0) {
		return fact(n-1.0) + fact(n-2.0);
    }
    else 
		return 1.0;
}