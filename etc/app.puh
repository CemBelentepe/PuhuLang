void main()
{
    double start = clock();
    int x = fibb(30);
    double elapsed = clock();
    print("Fibb(30): %i\nElapsed Time %d\n\n", x, elapsed);
}

int fibb(int n)
{
    if(n > 1)
        return fibb(n-1) + fibb(n-2);
    else
        return 1;
}