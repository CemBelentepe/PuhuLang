int add(int i, int j)
{
    return i + j;
}

void main()
{
    int res = (2).add(3);
    res = res.add3(res, 1);
    "%i\n".print(res);
    MyStruct& s = heap MyStruct;
    s.init(res, res.add(4));
    print("%i, %i \n", s->i, s->j);
}
